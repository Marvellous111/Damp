const getDexQuote: {} = (async (chainId: string, fromToken: string, toToken: string, amount: string, slippage: string) => {
  return {'data': 'empty string for now'};
})

async function getOkxHeaders() {}


export default {
  getDexQuote
}